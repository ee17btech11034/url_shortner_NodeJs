<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All URLs</title>
</head>
<body>
    <h2>All Urls</h2>
    <!-- local.shortId se bhi access kr skte hai it is useful when working with data fetched from Server and we not sure if everytime it will come like if local.shortId is useful-->
    <% urls.forEach(url => { %>
        <li> <%= url.shortId %> for link <%= url.redirectUrl %> </li>
    <% }) %>
</body>
</html>